const router = require("express").Router();
const path = require("path");

// these routes will be determined by the existing JS files in the PUBLIC folder

// GET /api/workouts/

// PUT /api/workouts/

// POST /api/workouts/
type = "cardio";
name = cardioNameInput;
distance = Number;
duration = Number;

type = "resistance";
name = nameInput;
weight = Number;
sets = Number;
reps = Number;
duration = Number;

// GET /api/workouts/range

module.exports = router;
